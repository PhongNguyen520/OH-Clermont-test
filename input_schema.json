{
  "title": "CountyFusion Scraper Input",
  "description": "Search input for GovOS County recorder websites (Clermont, Ross, Butler).",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "input": {
      "title": "Search configuration",
      "type": "object",
      "description": "Form search config. countyName: Clermont (default), Ross, or Butler. Records per page set to county max. fromDate/toDate: required, MM/dd/yyyy.",
      "editor": "json",
      "prefill": {
        "countyName": "Clermont",
        "fromDate": "01/01/2024",
        "toDate": "01/01/2024"
      }
    }
  },
  "required": ["input"]
}
